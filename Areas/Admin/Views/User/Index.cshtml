@model UserViewModel
@{
    ViewData["Title"] = "User not found";
}

<h3 class="mb-3">Admin Panel</h3>

<h5><a class="mt-2" asp-controller="Account" asp-area="default" asp-action="Register">Add a User</a></h5>

<table class="table table-striped table-bordered table-sm">
    <thead>
        <tr>
            <th>Username</th>
            <th>Roles</th>
            <th>Delete User</th>
            <th>Manage Roles</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Users == null || !Model.Users.Any())
        {
            <tr>
                <td colspan="4">No users found.</td>
            </tr>
        }
        else
        {
            @foreach (ApplicationUser user in Model.Users)
            {
                <tr>
                    <td>@user.UserName</td>
                    <td>
                        @if(user.RoleNames == null || !user.RoleNames.Any())
                        {
                            <div>No roles assigned</div>
                        }
                        else
                        {
                            @foreach (string roleName in user.RoleNames)
                            {
                                <div>@roleName</div>
                            }
                        }
                        
                    </td>
                    <td>
                        <a class="btn btn_mentii" asp-controller="User" asp-action="Edit" asp-route-id="@user.Id">Edit</a>
                        <a class="btn btn-danger" asp-controller="User" asp-action="Delete" asp-route-id="@user.Id">Delete</a>
                    </td>
                    <td>
                        @if(Model.Roles != null)
                        {   
                            @foreach(var role in Model.Roles)
                            {
                                if(user.RoleNames != null && role.Name != null && !user.RoleNames.Contains(role.Name))
                                {
                                        <a class="btn btn_mentii" asp-controller="User" asp-action="AddRole" asp-route-userId="@user.Id" asp-route-roleName="@role.Name">Add @role.Name</a>
                                }
                                else
                                {
                                    @if(user.UserName != "admin")
                                    {
                                            <a class="btn btn-danger" asp-controller="User" asp-action="RemoveRole" asp-route-userId="@user.Id" asp-route-roleName="@role?.Name">Remove @role?.Name</a>
                                    }
                                
                                }
                            }
                        }
                    </td>
                </tr>
            }
        }
    </tbody>
</table>